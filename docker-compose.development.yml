services:
  jam_backend:
    build:
      context: .
      dockerfile: Dockerfile.dev
    image: ivanmatcuka/jam_backend
    container_name: jam_backend
    ports:
      - '3001:3000'
    networks:
      - jam_network

  jam_postgres:
    image: postgres:13.3
    container_name: jam_postgres
    ports:
      - 5432
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      PGDATA: '/var/lib/postgresql/data/pgdata'
    volumes:
      - jam_pg_data:/var/lib/postgresql/data
    networks:
      - jam_network

networks:
  jam_network:
    driver: bridge
